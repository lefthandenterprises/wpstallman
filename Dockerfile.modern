# .NET 8 SDK on Ubuntu 24.04 ("noble") — already has the right deps (ICU, etc.)
FROM mcr.microsoft.com/dotnet/sdk:8.0-noble

ENV DOTNET_CLI_TELEMETRY_OPTOUT=1 \
    DEBIAN_FRONTEND=noninteractive

# (Optional) show glibc/OS
RUN getconf GNU_LIBC_VERSION && cat /etc/os-release

WORKDIR /src
